// Re-export the new CSS-only wrapper animation under the original name
// so that HeroSection doesn't need to change its import.
export { EgiWrapperAnimation as EgiTransformationAnimation } from './EgiWrapperAnimation';
